# dz-1
import random

while True:
    print("""
    === МЕНЮ ЗАВДАНЬ ===
    1 — Привітання з ім'ям та віком
    2 — Перевірка віку (вхід дозволено/заборонено)
    3 — Гра "Вгадай число"
    4 — Виведення чисел у діапазоні
    5 — Парні числа у зворотному порядку
    6 — Факторіал числа
    7 — Визначення оцінки за балами
    8 — Калькулятор
    0 — Вихід
    """)

    choice = input("Оберіть номер завдання: ")

    if choice == '0':
        print("До побачення! 👋")
        break

    elif choice == '1':
        name = input("Введіть ваше ім'я: ")
        age = input("Введіть ваш вік: ")
        print(f"Привіт {name}, тобі {age}!")

    elif choice == '2':
        age = int(input("Введіть ваш вік: "))
        print("Вхід дозволено!" if age > 18 else "Вхід заборонено!")

    elif choice == '3':
        number = random.randint(1, 10)
        print("Я загадав число від 1 до 10. У тебе є 3 спроби!")
        for i in range(3):
            guess = int(input("Введи число: "))
            if guess == number:
                print("Вітаю! Ти вгадав! 🎉")
                break
            print("Менше!" if guess > number else "Більше!")
        else:
            print(f"Ти не вгадав. Число було {number}.")

    elif choice == '4':
        a = int(input("Введіть число з: "))
        b = int(input("Введіть число по: "))
        print("Числа від", a, "до", b, ":", *range(a, b + 1))

    elif choice == '5':
        n = int(input("Введіть n: "))
        print("Парні числа у зворотному порядку:", *[i for i in range(n, 0, -1) if i % 2 == 0])

    elif choice == '6':
        n = int(input("Введіть число для факторіалу: "))
        f = 1
        for i in range(1, n + 1):
            f *= i
        print(f"Факторіал числа {n} = {f}")

    elif choice == '7':
        score = int(input("Введіть бали (0–100): "))
        if score < 0 or score > 100:
            print("Некоректне значення!")
        elif score < 50:
            print("Незадовільно")
        elif score < 70:
            print("Задовільно")
        elif score < 90:
            print("Добре")
        else:
            print("Відмінно")

    elif choice == '8':
        a = float(input("Введіть число a: "))
        b = float(input("Введіть число b: "))
        op = input("Оберіть дію (+, -, *, /): ")
        if op == '/' and b == 0:
            print("Ділення на нуль неможливе!")
        else:
            print("Результат:", eval(f"{a}{op}{b}"))

    else:
        print("Невірний вибір! Спробуйте ще раз.")

    input("\nНатисніть Enter, щоб продовжити...")
